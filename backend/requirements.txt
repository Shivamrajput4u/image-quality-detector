fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic-settings==2.7.0
python-multipart==0.0.20
python-dotenv==1.0.1

# Image analysis
opencv-python-headless==4.10.0.84
numpy==2.1.3
pillow==11.0.0

# Anomaly detection (autoencoder inference) — CPU build, install via:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch==2.5.1

# Testing
pytest==8.3.4
httpx==0.28.1

# ml/ scripts only (not needed to run the API itself) — dataset download +
# model training/evaluation. See ml/download_dataset.py and ml/train_autoencoder.py.
torchvision==0.20.1
scikit-learn==1.5.2
kaggle==1.6.17
